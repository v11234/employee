{
  "name": "employee-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "dependencies": {
    "pg": "^8.16.3"
  },
  "scripts": {
    "build": "npm run build --workspace frontend"
  }
}
